# @use-puncta/core

Technical ESM scaffold for Puncta. Exports the `Locale` type (`readonly id: string`)
and `localeId(locale)`. No React, adapter, locale registry, or domain functionality.

MIT licensed. Version 0.1.0-alpha.0 is a scaffold, not a stable API.
