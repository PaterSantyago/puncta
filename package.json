{
  "name": "puncta-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": "24.21.0"
  },
  "scripts": {
    "lint": "biome lint .",
    "format:check": "biome format . && prettier --check '**/*.{md,yml,yaml}'",
    "format": "biome format --write . && prettier --write '**/*.{md,yml,yaml}'",
    "typecheck": "tsc --noEmit",
    "build": "pnpm -r build",
    "test:install": "node scripts/install.mjs",
    "test": "pnpm test:install",
    "check": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm build && pnpm pack:check && pnpm test",
    "pack:check": "node scripts/pack.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@types/react": "19.3.0",
    "prettier": "3.9.6",
    "react": "19.3.0",
    "tsdown": "0.23.0",
    "typescript": "7.0.2",
    "verdaccio": "6.10.3"
  }
}
